# PhishGuard Server

Сервер для интеграции браузерного расширения с VK Bot. Проверяет ссылки на фишинг и отправляет уведомления через VK API.

## Локальная разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
python server.py